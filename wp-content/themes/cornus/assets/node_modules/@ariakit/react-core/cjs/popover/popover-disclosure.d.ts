import type { DialogDisclosureOptions } from "../dialog/dialog-disclosure.js";
import type { As, Props } from "../utils/types.js";
import type { PopoverAnchorOptions } from "./popover-anchor.js";
/**
 * Returns props to create a `PopoverDisclosure` component.
 * @see https://ariakit.org/components/popover
 * @example
 * ```jsx
 * const store = usePopoverStore();
 * const props = usePopoverDisclosure({ store });
 * <Role {...props}>Disclosure</Role>
 * <Popover store={store}>Popover</Popover>
 * ```
 */
export declare const usePopoverDisclosure: import("../utils/types.js").Hook<PopoverDisclosureOptions<"button">>;
/**
 * Renders a button that controls the visibility of the
 * [`Popover`](https://ariakit.org/reference/popover) component when clicked.
 * @see https://ariakit.org/components/popover
 * @example
 * ```jsx {2}
 * <PopoverProvider>
 *   <PopoverDisclosure>Disclosure</PopoverDisclosure>
 *   <Popover>Popover</Popover>
 * </PopoverProvider>
 * ```
 */
export declare const PopoverDisclosure: import("../utils/types.js").Component<PopoverDisclosureOptions<"button">>;
export interface PopoverDisclosureOptions<T extends As = "button"> extends PopoverAnchorOptions<T>, Omit<DialogDisclosureOptions<T>, "store"> {
}
export type PopoverDisclosureProps<T extends As = "button"> = Props<PopoverDisclosureOptions<T>>;
