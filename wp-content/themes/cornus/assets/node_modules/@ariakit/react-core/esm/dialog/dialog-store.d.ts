import * as Core from "@ariakit/core/dialog/dialog-store";
import type { DisclosureStoreFunctions, DisclosureStoreOptions, DisclosureStoreState } from "../disclosure/disclosure-store.js";
import type { Store } from "../utils/store.js";
export declare function useDialogStoreProps<T extends Core.DialogStore>(store: T, update: () => void, props: DialogStoreProps): T;
/**
 * Creates a dialog store to control the state of
 * [Dialog](https://ariakit.org/components/dialog) components.
 * @see https://ariakit.org/components/dialog
 * @example
 * ```jsx
 * const dialog = useDialogStore();
 *
 * <button onClick={dialog.toggle}>Open dialog</button>
 * <Dialog store={dialog}>Content</Dialog>
 * ```
 */
export declare function useDialogStore(props?: DialogStoreProps): DialogStore;
export interface DialogStoreState extends Core.DialogStoreState, DisclosureStoreState {
}
export interface DialogStoreFunctions extends Core.DialogStoreFunctions, DisclosureStoreFunctions {
}
export interface DialogStoreOptions extends Core.DialogStoreOptions, DisclosureStoreOptions {
}
export interface DialogStoreProps extends DialogStoreOptions, Core.DialogStoreProps {
}
export interface DialogStore extends DialogStoreFunctions, Store<Core.DialogStore> {
}
