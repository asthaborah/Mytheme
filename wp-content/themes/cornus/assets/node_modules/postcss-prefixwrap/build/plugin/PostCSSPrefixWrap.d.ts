import { PostCSSContainer } from "../Types";
export declare const PLUGIN_NAME = "postcss-prefixwrap";
export interface PostCSSPrefixWrapOptions {
    ignoredSelectors?: (string | RegExp)[];
    prefixRootTags?: boolean;
    whitelist?: Array<string>;
    blacklist?: Array<string>;
    nested?: string;
}
export default class PostCSSPrefixWrap {
    private readonly blacklist;
    private readonly ignoredSelectors;
    private readonly isPrefixSelector;
    private readonly prefixRootTags;
    private readonly prefixSelector;
    private readonly whitelist;
    private readonly nested;
    constructor(prefixSelector: string, options?: PostCSSPrefixWrapOptions);
    prefixRoot(css: PostCSSContainer): void;
    prefix(): (css: PostCSSContainer) => void;
}
